<p>Welcome <%= @email %>!</p>

<p>You can confirm your account email through the link below:</p>

<%= render 'layouts/mailer_button', :link => confirmation_url(@resource, confirmation_token: @token), :link_text => 'Confirm my account' %>
